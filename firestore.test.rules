rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for testing
    // Tests connect to emulator as regular clients (not Admin SDK)
    // so rules are enforced. Allow all operations for test simplicity.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
