rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All client access is denied - only backend Admin SDK can write
    // This is appropriate when all access goes through a REST API
    // that uses Firebase Admin SDK (which bypasses these rules)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
